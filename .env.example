# ===========================================
# DEHARRI USER MANAGEMENT SERVICE - SECRETS
# ===========================================
# Copy this file to .env and fill in your values
# Command: cp .env.example .env
# ===========================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=deharri-user-mgmt-db
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# JWT Secret (generate using: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here

# AWS Credentials (get from AWS IAM console)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# Mail Configuration (Gmail App Password)
# To get an app password: https://myaccount.google.com/apppasswords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
