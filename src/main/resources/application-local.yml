spring:
  datasource:
    password: 123456
    url: jdbc:postgresql://localhost:5432/deharri-user-mgmt-db
    username: postgres

  data:
    jdbc:
      dialect: postgresql

  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  port: 8088